"name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }",
  "private": true,
  "scripts": {
    {{#lint}}
    "lint": "eslint --ext .js ./src",
    {{/lint}}
    {{#sentry}}
    "sentry": "node scripts/sentry.js",
    {{/sentry}}
    "start": "node scripts/server.js",
    "build": "rm -rf dist/* && node scripts/build.js",
    "deploy": "deploy-sftp",
    "hotdeploy": "node scripts/deploy.js"
  },